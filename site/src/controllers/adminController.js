const { products } = require('../data/products_db');

module.exports = {
    add : (req,res) => {
        res.render();
    },
    store : (req,res) => {
        res.render();
    },
    update : (req,res) => {
        res.render();
    },
    delete : (req,res) => {
        res.render();
    }
}